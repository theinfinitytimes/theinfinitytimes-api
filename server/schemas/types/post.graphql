type Post {
    title: String!
    id: Int!
    body: String!
    author: ID!
    dateCreated: String!
    lastModified: String
    modifiedBy: ID
    picture: String
    tag: Int
}

input PostInput {
    title: String!
    body: String!
    author: ID!
    picture: String
    tag: Int
}

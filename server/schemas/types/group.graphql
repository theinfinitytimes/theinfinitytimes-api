type Group {
    _id: ID!
    id: Int!
    name: String!
    members: [ID!]!
}

input GroupInput {
    id : Int
    name: String!
    members: [ID!]
}

input GroupDeleteInput {
    _id: ID!
    id: Int!
    name: String!
}

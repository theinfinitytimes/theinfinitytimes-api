type Author {
    _id: ID!
    account: Account!
    description: String!
    posts: [ID!]!
}

input AuthorInput {
    _id: ID
    account: ID!
    description: String!
    posts: [ID!]
}

input AuthorDeleteInput {
    _id: ID!
    account: ID!
}
